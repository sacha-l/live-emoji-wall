node_modules/
dist/
*.tsbuildinfo
*.car

# Local env files (never commit real secrets; keep .env.example tracked)
.env*
!.env.example

# OS cruft
.DS_Store

# AI session / per-developer local state (the tracked .claude/commands/* stay)
.claude/sessions/
.claude/cache/
.claude/settings.local.json

# Product SDK skills — fetched by ./setup.sh, not committed (kept fresh, never stale)
.claude/skills/

# Playwright MCP scratch output
.playwright-mcp/
